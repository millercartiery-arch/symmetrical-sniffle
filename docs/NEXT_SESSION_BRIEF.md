# 下次会话交接

> 这个文件手动维护，只写“人需要知道的上下文”。
> 自动状态请看 `docs/CURRENT_STATUS.md`。

## 当前目标

- 保持桌面端、前端、后端、账户导入扩展和本地 TN 协议账户导入工具的交付状态可追溯，避免下次打开后重复排查“上次做到哪里”。

## 最近完成

- 2026-03-19 的一批主要工作已记录在 `docs/工作日记-2026-03-19.md`。
- 已完成前端一轮 UI 统一、账户导入扩展并入、导入校验旧逻辑修正、扩展打包与桌面打包验证。
- 2026-03-20 已补建新的本地 Git 仓库，并做了当前测试态基线提交：`80c02f5`。
- 已确认当前仓库内的 TN 协议发送链路、发送检查脚本、代理检查脚本均已在位。
- 已确认本地使用的 TN 协议账户导入工具保留在 `frontend/tools/import_tn_accounts.py`。

## 当前状态

- 仓库已经有首次基线提交，不再是无提交历史状态。
- 当前项目已进入实测准备阶段，按口径“链路已基本打通，只差 IP/代理提交后做 TN 协议发送实测”理解即可。
- 如果 AI 会话中断，先以本文件和 `docs/CURRENT_STATUS.md` 为准，不要只依赖聊天历史。

## 下一步优先事项

- 准备可用 `IP/代理`，用于 TN 协议发送链路实测。
- 完成阶段性核对后，执行一次 `npm run handoff:update`，刷新客观状态快照。
- 如果要推送到远端，确保只绑定新的仓库地址，不要混入旧仓库历史或旧目录代码。

## 阻塞 / 注意事项

- OneDrive 工作目录下的 Rust `target` 输出目录可能不可写；桌面打包时优先使用外部 `CARGO_TARGET_DIR`。
- 当前主要阻塞点是 `IP/代理` 资源，而不是 TN 协议发送代码本身。
- `frontend/tools/import_tn_accounts.py` 是本地工具，`browser-extension/tn-account-importer/` 是浏览器扩展，不要混淆。

## 下次打开可以直接说

先读取 `docs/NEXT_SESSION_BRIEF.md`、`docs/CURRENT_STATUS.md` 和 `docs/工作日记-2026-03-20.md`，基于里面的状态继续上次工作，优先按“只差 IP/代理即可测试 TN 协议发送链路”的现状推进，不要回退到旧整理阶段。
